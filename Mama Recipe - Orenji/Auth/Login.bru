meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{url}}/user/login
  body: json
  auth: inherit
}

body:json {
  {
      "email": "real-email@example.com",
      "password": "good-password"
  }
  
  // To Login need to verify email.
}

settings {
  encodeUrl: true
}
